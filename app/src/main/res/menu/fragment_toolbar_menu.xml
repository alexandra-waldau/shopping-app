<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/delete_all"
        android:drawableLeft="@drawable/ic_location_icon"
        android:icon="@drawable/ic_delete_forever"
        android:title="@string/delete_all"
        app:showAsAction="ifRoom|withText" />

    <item
        android:id="@+id/share_list"
        android:icon="@drawable/ic_send"
        android:title="@string/share_list"
        app:showAsAction="ifRoom|withText" />

    <item
        android:id="@+id/show_subtitle"
        android:drawableLeft="@drawable/ic_location_icon"
        android:icon="@drawable/ic_numbers_list"
        android:title="@string/show_subtitle"
        app:showAsAction="never|ifRoom|withText" />
    <item
        android:id="@+id/add_items"
        android:title="@string/add_items"
        android:color="@color/colorRedBright"
        app:showAsAction="never" />

</menu>